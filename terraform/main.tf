terraform {
  required_providers {
    google = {
      source = "hashicorp/google"
      version = "7.16.0"
    }
  }
}

provider "google" {
  project     = "terraform-setup-485500"
  region      = "us-central1"
}

resource "google_storage_bucket" "data-bucket" {
  name          = "terraform-setup-485500-data-bucket"
  location      = "US"
  force_destroy = true

  lifecycle_rule {
    condition {
      age = 1
    }
    action {
      type = "AbortIncompleteMultipartUpload"
    }
  }
}